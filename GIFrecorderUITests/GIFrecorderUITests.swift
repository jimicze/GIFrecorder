import XCTest

final class GIFrecorderUITests: XCTestCase {

    override func setUpWithError() throws {
        continueAfterFailure = false
    }

    func testAppLaunches() throws {
        // UI tests require real screen recording permission
        // This test just verifies the app can launch
        // Full UI testing is limited due to screen recording permission requirements
    }
}
